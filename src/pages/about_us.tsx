import React, { ReactElement } from 'react'

interface Props {
    
}

export default function about_us({}: Props): ReactElement {
    return (
        <div>
            this is about us
        </div>
    )
}
