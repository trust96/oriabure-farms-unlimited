import React from "react";
import Head from "../content/modules/Head/Head";

interface Props {}

const contact_us = (props: Props) => {
  return (
    <div className="pages">
      <Head>Services</Head>
    </div>
  );
};

export default contact_us;
